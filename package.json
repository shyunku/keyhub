{
  "main": "public/electron/main.js",
  "name": "password-hub",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "concurrently": "6.1.0",
    "node-sass": "6.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-icons": "4.2.0",
    "react-router-dom": "5.2.0",
    "react-scripts": "4.0.3",
    "wait-on": "5.3.0",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "react": "set BROWSER=none&& yarn start",
    "electron": "set ELECTRON_START_URL=http://localhost:3000 && wait-on http://localhost:3000 && electron .",
    "all": "concurrently \"set BROWSER=none&& yarn start\" \"set ELECTRON_START_URL=http://localhost:3000 && wait-on http://localhost:3000 && electron .\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "electron": "12.0.7",
    "electron-builder": "22.10.5"
  }
}
